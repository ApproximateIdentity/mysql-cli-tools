all
    * create a separate error codes file

setup
    * add extra info to setup.py. see the following url for info:
      https://docs.python.org/2/distutils/setupscript.html#additional-meta-data

mv-mysql
    * add ability to move tables
    * add ability to read database info from configuration files

ls-mysql
    * add ability to list tables inside a database

Wishlist
--------
    * add `du-mysql` function. use the following sql query:

SELECT table_schema                                        "DB Name", 
   Round(Sum(data_length + index_length) / 1024 / 1024, 1) "DB Size in MB" 
FROM   information_schema.tables 
GROUP  BY table_schema; 
